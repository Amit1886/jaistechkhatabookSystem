import json
import logging

logger = logging.getLogger(__name__)


def generate_ai_suggestions(project_id: int) -> list:
    return []


def improve_layout(project_id: int) -> dict:
    return {"status": "ok"}
