from django.apps import AppConfig


class PlatformReportingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.platform.reporting"
    label = "platform_reporting"
    verbose_name = "Platform Reporting & Analytics"

