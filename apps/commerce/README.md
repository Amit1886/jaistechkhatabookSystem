# Commerce Domain

Owns order lifecycle, invoices, storefront, POS, vendor marketplace, coupons, and channel sales.

Initial source apps:
- `commerce`
- `orders`
- `pos`
- `storefront`
- `vendors`

This should remain backward compatible with existing `/commerce/`, `/store/`, and `/api/v1/orders/` routes while internals are split.
